{
  "$schema": "https://raw.githubusercontent.com/ow-mods/owml/master/schemas/config_schema.json",
  "enabled": true,
  "settings": {
    "extendLoadingTime": {
      "title": "Extend Loading Time",
      "type": "toggle",
      "value": false,
      "tooltip": "Artifically extends the loading time when starting an expedition. Useful for computers that load the game too quickly to see the animation."
    }
  }
}
